import React, { Component } from 'react';

const Home = function () {
  // This should display a div with the class "home" and the text "Welcome to our page"
}

const About = function () {
  // This should display a div with the class "about" and the text "It's great here"
}

class SpotCheck2 extends Component {

  // Your SpotCheck2 should render the Home Component,
  // the About Component, and a Toggle Button
  // Either the Home or About component should be rendered 
  // when you press the toggle button
  // You must use State and SetState

  render() {
    return (
      <div >

      </div>
    );
  }
}

export default SpotCheck2;
